CREATE TABLE TELEFONOS(
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL,
    USUARIO_ID UUID NOT NULL,
    CITY_CODE CHARACTER VARYING(255) NOT NULL,
    COUNTRY_CODE CHARACTER VARYING(255) NOT NULL,
    NUMBER CHARACTER VARYING(255) NOT NULL
);
ALTER TABLE TELEFONOS ADD CONSTRAINT CONSTRAINT_D PRIMARY KEY(ID);
CREATE MEMORY TABLE USUARIOS(
    ID UUID NOT NULL,
    EMAIL CHARACTER VARYING(255) NOT NULL,
    NAME CHARACTER VARYING(255) NOT NULL,
    PASSWORD CHARACTER VARYING(255) NOT NULL
);
ALTER TABLE USUARIOS ADD CONSTRAINT USUARIOS_PK PRIMARY KEY(ID);
ALTER TABLE USUARIOS ADD CONSTRAINT USUARIOS_UNIQUE UNIQUE NULLS DISTINCT (EMAIL);
ALTER TABLE TELEFONOS ADD CONSTRAINT TELEFONOS_PK FOREIGN KEY(USUARIO_ID) REFERENCES USUARIOS(ID) NOCHECK;